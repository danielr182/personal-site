import { TimelineItemDetailDirective } from './timeline-item-detail.directive';

describe('TimelineItemDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineItemDetailDirective();
    expect(directive).toBeTruthy();
  });
});
